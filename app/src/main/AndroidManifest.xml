<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vdetails"
    android:installLocation="auto">

    <uses-feature android:name="android.hardware.camera"></uses-feature>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="VDetails"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".MainApplication">
        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="ocr" />
        <activity android:name=".HomeActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.vdetails.BaseClass"
            android:label="@string/title_activity_main" >
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name="com.example.vdetails.OcrCaptureActivity"
            android:label="Read Text" />
        <activity android:name=".openCamera"
            android:label="@string/app_name"/>
        <activity android:name=".uploadPic"/>
        <activity android:name=".takePic"/>
        <activity android:name=".uploadVid"/>
        <activity android:name=".vehicleDetails"/>

    </application>

</manifest>